# Implementation Tasks: VCR Deterministic Record/Replay for HeadlessInAppWebView

This file is generated by `/speckit.tdd.plan` to make test tasks mandatory and bind them to behavior IDs. Every implementation task must have its test task listed first and marked with the behavior marker `[A#]` / `[U#]`.

**Hard rules:**
- Tests are NOT optional. The "OPTIONAL" and "only if tests requested" qualifiers are removed.
- Each behavioral task carries its behavior ID in brackets (e.g., `[A1]`) — this marker is load-bearing for `/speckit.tdd.run` and `/speckit.implement`.
- Task IDs continue the existing sequence. Never renumber or reword checked tasks.
- Characterization tasks (if any) come before the tasks that change the code they cover.

## Phase 1: Acceptance Tests (Outer Loop)

- [ ] **T-001** `[A1]` Test: Record mode captures each navigation's URL, served HTML, and cookie snapshot into a cassette entry
- [ ] **T-002** `[A1]` Implement: VCR wrapper record mode — navigation capture with URL, HTML, cookies
- [ ] **T-003** `[A2]` Test: Record mode captures XHR/fetch network-capture events (request/response/body) into the cassette
- [ ] **T-004** `[A2]` Implement: VCR wrapper record mode — network event capture
- [ ] **T-005** `[A3]` Test: Completed recording is written as gzipped JSON with a declared format version
- [ ] **T-006** `[A3]` Implement: Cassette writer — gzipped JSON with format version
- [ ] **T-007** `[A4]` Test: Replay mode intercepts loadUrl and serves recorded HTML via loadData with zero outbound network calls
- [ ] **T-008** `[A4]` Implement: VCR wrapper replay mode — loadUrl interception and loadData injection
- [ ] **T-009** `[A5]` Test: Replay mode synthesizes recorded network-capture events so downstream logic runs unmodified
- [ ] **T-010** `[A5]` Implement: VCR wrapper replay mode — network event synthesis
- [ ] **T-011** `[A6]` Test: Replay mode returns recorded HTML for getHtml() without changing consuming logic
- [ ] **T-012** `[A6]` Implement: VCR wrapper replay mode — getHtml() compatibility
- [ ] **T-013** `[A7]` Test: Repeating the same replay mission 10 times yields identical getHtml() and network-entry ordering
- [ ] **T-014** `[A7]` Implement: Deterministic replay — identical outcomes across runs
- [ ] **T-015** `[A8]` Test: Request with differing normalized body but matching URL resolves via best-match fallback deterministically
- [ ] **T-016** `[A8]` Implement: Best-match fallback for URL-match with body differences
- [ ] **T-017** `[A9]` Test: Unmatched request in replay mode fails hard by default with clear error naming the unmatched key
- [ ] **T-018** `[A9]` Implement: Hard unmatched policy — fail with clear error
- [ ] **T-019** `[A10]` Test: Unmatched request with soft policy logs warning and returns empty response instead of failing
- [ ] **T-020** `[A10]` Implement: Soft unmatched policy — warn and return empty
- [ ] **T-021** `[A11]` Test: Redaction hook scrubs Authorization header values before cassette is written
- [ ] **T-022** `[A11]` Implement: Redaction hook — Authorization header scrubbing
- [ ] **T-023** `[A12]` Test: Redaction hook scrubs cookie values from cassette while retaining non-secret metadata
- [ ] **T-024** `[A12]` Implement: Redaction hook — cookie value scrubbing with metadata retention
- [ ] **T-025** `[A13]` Test: Custom redaction rule is applied to matching headers/cookies before cassette write
- [ ] **T-026** `[A13]` Implement: Custom redaction rule support
- [ ] **T-027** `[A14]` Test: Cassette loader validates format version and rejects incompatible/unknown versions with clear error
- [ ] **T-028** `[A14]` Implement: Cassette loader — format version validation
- [ ] **T-029** `[A15]` Test: Captured response body exceeding 50 KB cap is truncated at configured limit
- [ ] **T-030** `[A15]` Implement: Response body size cap using existing capture settings
- [ ] **T-031** `[A16]` Test: Cassette remains gzip-compressed and under reasonable size threshold for repo commit
- [ ] **T-032** `[A16]` Implement: Gzip compression and size optimization

## Phase 2: Integration & Validation

- [ ] **T-033** `[A1-A16]` Test: End-to-end record→replay round-trip of scripted product-page session yields identical getHtml() output and network entries (SC-001)
- [ ] **T-034** `[A11-A13]` Test: Redaction test confirms no auth header value or cookie value in committed cassette (SC-002)
- [ ] **T-035** `[A7]` Test: Replaying same cassette 10 times yields identical mission outcomes (SC-003)
- [ ] **T-036** `[A3, A14-A16]` Test: Example cassettes for at least 3 real retailer pages committed under test/fixtures/ load/replay successfully (SC-004)
- [ ] **T-037** `[A15]` Test: Recorded response larger than 50 KB body cap stores capped body (SC-005)
- [ ] **T-038** `[A14]` Test: Loader rejects cassettes with unsupported/unknown format version with clear actionable error (SC-006)

## Phase 3: Edge Cases & Platform Independence

- [ ] **T-039** Test: Multiple navigations to same URL with different content resolve to correct capture via occurrence order
- [ ] **T-040** Test: Requests not captured during record become unmatched calls handled by unmatched policy
- [ ] **T-041** Test: Redaction disabled/empty hook emits warning or refuses commit-path write
- [ ] **T-042** Test: Gzip/JSON parse failure yields clear "corrupt/unsupported" error
- [ ] **T-043** Test: Best-match fallback finding nothing falls through to unmatched policy
- [ ] **T-044** Test: Empty cassette makes any loadUrl an unmatched hard failure by default
- [ ] **T-045** Test: Cassette recorded on one platform replays identically on another (platform independence)

## Notes

- All test tasks must be observed failing first (red phase) before implementation begins.
- The test list in `tdd/test-list.md` is the source of truth for behavior definitions and traceability.
- The cycle log in `tdd/cycle-log.md` records the evidence for each red→green→refactor cycle.
- This feature has no `plan.md` yet; inner-loop tasks will be added when plan.md exists and `/speckit.tdd.plan refresh` is run.

## Phase 4: TDD remediation

- [ ] **T-046** `[Finding 1]` Create first failing acceptance test for A1 (record mode captures navigation URL, HTML, cookies). Run: `flutter test <new_test_file> --plain-name "A1"` must fail (red).
- [ ] **T-047** `[Finding 1]` Create first failing acceptance test for A4 (replay mode intercepts loadUrl, serves HTML via loadData, zero network). Run: `flutter test <new_test_file> --plain-name "A4"` must fail (red).
- [ ] **T-048** `[Finding 2]` Implement minimal VCR wrapper source (`lib/src/vcr/`) to make A1 test pass (green). Run: `flutter test <test_file> --plain-name "A1"` must pass.
- [ ] **T-049** `[Finding 2]` Implement minimal replay mode to make A4 test pass (green). Run: `flutter test <test_file> --plain-name "A4"` must pass.
- [ ] **T-050** `[Finding 3]` Add cycle log entries for A1 and A4 cycles (red command + output, green change, refactor). Verify: `cat tdd/cycle-log.md` shows Cycle 1 and Cycle 2 entries.
- [ ] **T-051** `[Finding 1,2,3]` Continue TDD loop for remaining 14 behaviors (A2, A3, A5-A16) per test list order. Each cycle: write failing test, make pass, refactor, log.