// Auto-generated by Zorphy (via zfa entity create) — post-processed for
// zikzak_inappwebview: constructor defaults + int action wire format.

import 'package:zorphy_annotation/zorphy_annotation.dart';
import '../enums/index.dart';

part 'js_before_unload_response.zorphy.dart';
part 'js_before_unload_response.g.dart';

///Class that represents the response used by the [PlatformWebViewCreationParams.onJsBeforeUnload] event to control a JavaScript alert dialog.
@Zorphy(
  kind: ZorphyKind.valueObject,
  generateJson: true,
  generateCompareTo: true,
)
abstract class $JsBeforeUnloadResponse {
  ///Message to be displayed in the window.
  @JsonKey(defaultValue: '')
  String get message;

  ///Title of the confirm button.
  @JsonKey(defaultValue: '')
  String get confirmButtonTitle;

  ///Title of the cancel button.
  @JsonKey(defaultValue: '')
  String get cancelButtonTitle;

  ///Whether the client will handle the alert dialog.
  @JsonKey(defaultValue: false)
  bool get handledByClient;

  ///Action used to confirm that the user hit confirm or cancel button.
  @JsonKey(
    defaultValue: JsBeforeUnloadResponseAction.CONFIRM,
    fromJson: _actionFromJson,
    toJson: _actionToJson,
  )
  JsBeforeUnloadResponseAction? get action;
}

JsBeforeUnloadResponseAction? _actionFromJson(Object? value) {
  if (value is! int) return null;
  final index = value;
  return index >= 0 && index < JsBeforeUnloadResponseAction.values.length
      ? JsBeforeUnloadResponseAction.values[index]
      : null;
}

Object? _actionToJson(JsBeforeUnloadResponseAction? action) => action?.index;
